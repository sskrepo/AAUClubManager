# OpenAPI Changes (per phase)

One file per phase: `phase-{N}.md`

Owned by Architect. Filed AFTER Gate 1 (PDD + mocks) approved. Cannot be approved until Gate 1 passes (Architect can draft in parallel for efficiency, but formal Gate 2 submission must reflect approved PDD).

Format and protocol: [dev-agent-team/shared/phase-deliverables-protocol.md](../../../dev-agent-team/shared/phase-deliverables-protocol.md)

## Approval gate (Gate 2)

User approval syntax:
- `OPENAPI-PHASE-N: approved` — approves the phase's API surface
- `OPENAPI-PHASE-N: needs changes — {description}` — request edits

After Gate 2 passes, Dev Manager picks up engineering work.

## What's here

(empty — first spec changes filed at start of Phase 1, after PDD approved)

## Authoritative spec

`api/openapi.yaml` is the running source of truth. Per-phase docs here summarize what changes that file in each phase, for user review.
